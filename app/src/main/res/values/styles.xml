<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!--
        <item name="android:fontFamily">@font/cafe24surround</item>
        <item name="android:font" tools:targetApi="o">@font/cafe24surround</item>
        -->
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:windowBackground">@drawable/splash</item> </style>

    <style name="Dialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@drawable/dialog_round</item>
    </style>

    <style name="DialogTransparent" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@drawable/dialog_round_transparent</item>
    </style>

</resources>